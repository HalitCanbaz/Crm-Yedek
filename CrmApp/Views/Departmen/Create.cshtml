@model CrmApp.Models.Departman

@{
    ViewData["Title"] = "Create";
}

<div class="card">
    <div class="card-body">
        <div class="row">
            <div class="col-md-4">
                <h5>Departman Oluştur</h5>
                <hr />

                <form asp-action="Create">
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                    <div class="form-group">
                        <label asp-for="DepartmanName" class="control-label">Departman Adı</label>
                        <input asp-for="DepartmanName" class="form-control" />
                        <span asp-validation-for="DepartmanName" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <input type="submit" value="Oluştur" class="btn btn-secondary" />
                    </div>
                </form>
            </div>
        </div>

        <div>
            <a asp-action="Index" class="btn btn-info">Listeye Geri Dön</a>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
        }
